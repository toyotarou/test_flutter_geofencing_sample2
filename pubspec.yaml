name: test_flutter_geofencing_sample2
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  native_geofence: ^1.2.1
  flutter_local_notifications: ^20.1.0
  permission_handler: ^12.0.1
  geolocator: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
